<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $sub = App\Models\Submission::where('status', '!=', 'rejected')->first(); if($sub) { $sub->fill(['status'=>'rejected', 'rejection_note'=>'TESTING_123']); $sub->save(); echo 'SAVED: ' . $sub->rejection_note; } else { echo 'No sub found'; }
